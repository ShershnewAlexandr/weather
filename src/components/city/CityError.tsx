import React from 'react';
import styles from './CityError.module.scss';

const CityError: React.FC = () => {
  return <div className={styles.CityError}>some went wrong!</div>;
};

export { CityError };
